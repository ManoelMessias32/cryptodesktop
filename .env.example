# Crie um arquivo chamado .env (sem .example) e adicione suas variáveis de ambiente

# Chave privada da carteira que tem permissão para mintar (criar) os tokens BDG.
# IMPORTANTE: Mantenha esta chave em segredo e nunca a envie para o controle de versão (git).
SERVER_PRIVATE_KEY=SUA_CHAVE_PRIVADA_AQUI

# URL do provedor RPC para a rede BNB Testnet
RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/
